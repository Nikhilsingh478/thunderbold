I need you to do a FULL SECURITY + FUNCTIONAL AUDIT of this project properly. Do NOT make assumptions. Do NOT give vague summaries. I want actual code-level verification with exact files, exact logic, exact findings, and exact fixes.

IMPORTANT:
Take your time.
Do NOT rush.
Do NOT do partial fixes.
Do NOT give generic explanations.
I want production-grade verification and implementation.

==================================================
PART 1 — ADMIN AUTHENTICATION / SECURITY AUDIT
==============================================

The MOST important thing I need checked right now is:

HOW were you able to access/login into the admin panel during previous testing sessions when the admin panel is supposed to only allow access to 2 specific email IDs?

I need a FULL TRACE of the auth flow.

You MUST investigate EVERYTHING related to authentication.

---

## CHECK ALL OF THESE THINGS:

1. Find EVERY admin auth related file:

* Firebase auth files
* Middleware
* Protected routes
* Admin checks
* JWT/token verification
* Role/email validation
* Route guards
* Session persistence
* Local storage usage
* Cookies
* API auth middleware

Search ALL of:

* api/
* src/
* middleware/
* auth/
* lib/
* hooks/
* utils/

---

2. I need you to explain EXACTLY:

How can admin access currently happen?

Is admin protection:

* frontend only?
* backend enforced?
* both?
* partially broken?
* bypassable?

---

3. CHECK api/_lib/firebaseAdmin.js PROPERLY

I want exact findings for:

* Is FIREBASE_SERVICE_ACCOUNT required?
* What happens when it is missing?
* Is jwt.decode() being used?
* Is verifyIdToken() being skipped?
* Is there fallback insecure logic?

If there is ANY insecure fallback:
REMOVE IT COMPLETELY.

I do NOT want:
jwt.decode(token)

ANYWHERE as fallback auth verification.

If Firebase Admin SDK is unavailable:
AUTH MUST FAIL.
NOT bypass.
NOT decode-only.
NOT soft-verify.

---

4. VERIFY ACTUAL SECURITY MODE

I need you to tell me:
Given the CURRENT environment variables configured in this Replit project:

Is the admin authentication cryptographically secure RIGHT NOW or NOT?

I want factual verification.
Not assumptions.

---

5. CHECK IF ADMIN AUTH CAN BE BYPASSED

I want you to actively verify:

* can someone manually fake a token?
* can someone inject an email?
* can frontend localStorage be modified?
* can admin routes open without proper backend verification?
* are APIs actually protected server-side?
* is admin validation only happening in React UI?
* can a crafted JWT bypass auth because of jwt.decode?

---

6. TRACE HOW YOU (REPLIT) ACCESSED ADMIN PANEL EARLIER

I want a REAL explanation.

Check:

* whether existing browser sessions were reused
* whether localStorage auth state already existed
* whether auth persistence auto-logged in
* whether admin APIs were actually unsecured
* whether auth middleware is failing open instead of failing closed

I want the REAL technical explanation.

---

7. SEARCH FOR HARDCODED SECRETS

Search the ENTIRE project for:

* Firebase private keys
* service account JSON
* API secrets
* JWT secrets
* hardcoded credentials
* exposed Mongo URIs
* exposed Cloudinary secrets

I want exact file paths.

---

8. FIX AUTH PROPERLY

I want production-grade admin auth.

Requirements:

* Backend MUST verify Firebase ID tokens cryptographically
* Use Firebase Admin SDK only
* No insecure fallback logic
* Unauthorized requests must fail with proper HTTP status
* Admin routes must be protected server-side
* Allowed admin emails must be enforced server-side
* Frontend-only checks are NOT enough

If environment variables are missing:
RETURN ERROR.
DO NOT bypass.

---

9. AFTER FIXING:
   I want you to explain:

* what was insecure before
* what exactly you changed
* why it is secure now
* what environment variables are required
* whether production deployment is safe now

==================================================
PART 2 — KURTA SECTION FIXES
============================

Now continue and COMPLETE the unfinished Kurta implementation properly.

Current understanding:
The frontend already partially supports Kurta products.
BUT backend/API validation still requires categoryId.
That is causing product creation failure.

---

## KURTA REQUIREMENTS:

1. Kurta section MUST appear AFTER T-Shirts section on homepage.

2. Kurta section is DIFFERENT from other sections:

* NO categories
* Products are directly attached to Kurta section
* Admin directly adds products into Kurta
* No nested category flow

3. FIX backend logic properly:
   Currently API rejects Kurta products because categoryId is required.

You need to:

* make categoryId optional ONLY for Kurta
* keep existing validation for all other sections
* do NOT break existing categories

4. Verify:

* admin can add Kurta products
* products save correctly
* homepage displays them
* product pages work
* filtering/search doesn't break
* mobile layout works
* no console errors
* no API validation errors

5. I do NOT want hacky conditions scattered everywhere.
   Implement this cleanly using maintainable scalable logic.

==================================================
PART 3 — ANALYTICS MOBILE RESPONSIVENESS
========================================

The admin analytics dashboard is still broken on mobile.

Fix ALL responsiveness issues properly.

CHECK:

* charts overflow
* graph widths
* KPI cards
* padding/margins
* table responsiveness
* mobile scrolling
* text overlap
* touch usability

Requirements:

* fully usable on phones
* no horizontal breaking
* proper responsive grid behavior
* no compressed unreadable charts

Do this properly.
Not superficial CSS patches.

==================================================
PART 4 — PURCHASE PRICE / FALLBACK LOGIC
========================================

There is existing old logic where MRP was auto-generated using a percentage markup.

I want this behavior updated carefully.

Requirements:

1. Existing old auto-generated prices should remain as fallback/backward compatibility.

2. BUT:
   If admin manually enters a purchase price/new official value:
   THAT value becomes the authoritative value.

3. Do NOT break old products.

4. Implement this in a scalable way:

* clean pricing utilities
* centralized logic
* avoid duplicated calculations

==================================================
PART 5 — BRANDS BANNER UI
=========================

Continue the brighter "View All Brands" banner redesign.

You already mentioned:

* amber/gold glow
* brighter UI
* hover improvements

Now finalize it properly:

* responsive
* polished
* premium looking
* not oversaturated
* theme-consistent
* accessible contrast

==================================================
PART 6 — CODE QUALITY REQUIREMENTS
==================================

VERY IMPORTANT:

Do NOT:

* add redundant code
* add duplicated conditions
* create hacky patches
* scatter business logic
* create inconsistent state handling

I want:

* maintainable architecture
* scalable structure
* reusable helpers/utilities
* proper separation of concerns
* production-grade implementation

==================================================
PART 7 — FINAL REPORT (MANDATORY)
=================================

At the end I want a COMPLETE REPORT.

The report MUST include:

1. Every file changed

2. Exact security findings

3. Exact auth vulnerabilities found

4. Whether admin auth was bypassable earlier

5. Whether insecure jwt.decode fallback existed

6. Whether backend auth is now cryptographically secure

7. Required environment variables

8. Exact Kurta fixes

9. Exact mobile responsiveness fixes

10. Exact pricing logic fixes

11. Any remaining risks/issues

12. Verification steps performed

13. Actual test results

14. Whether production deployment is safe now

DO NOT give vague summaries.
DO NOT skip details.
DO NOT partially complete things.

Verify everything properly before claiming completion.
