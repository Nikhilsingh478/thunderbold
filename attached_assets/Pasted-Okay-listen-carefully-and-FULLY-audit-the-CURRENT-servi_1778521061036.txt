Okay, listen carefully and FULLY audit the CURRENT service worker + deployment setup before making ANY changes.

The PWA implementation is STILL NOT fully correct.

Current PWABuilder results show:

* Manifest improvements ARE being detected
* App capabilities ARE being detected
* BUT the Service Worker is STILL NOT detected

This means:
the manifest work is mostly successful,
BUT the ACTUAL production service worker infrastructure is STILL broken or improperly exposed.

This is now the FINAL focused debugging pass specifically for:

* service worker detection
* production SW activation
* Vercel deployment compatibility
* PWABuilder compatibility
* installability verification

DO NOT treat this as a cosmetic task.
This is now a low-level infrastructure debugging task.

==================================================
CURRENT VERIFIED STATUS
=======================

WORKING:

* Manifest improvements detected
* Shortcuts detected
* Launch Handler detected
* Share Target detected
* Window Controls Overlay detected
* Splash screen improvements implemented
* PWA visuals improved

NOT WORKING:

* PWABuilder STILL says:
  “No Service Worker”
* Offline support not detected
* Service worker logic not detected

This means the app is STILL not a fully valid production PWA yet.

==================================================
MOST IMPORTANT GOAL
===================

PWABuilder MUST successfully detect:

* Has Service Worker
* Has Logic
* Offline Support

At minimum.

==================================================
PHASE 1 — REAL PRODUCTION AUDIT
===============================

Before changing ANYTHING:

Audit the REAL production deployment behavior.

DO NOT rely only on:

* local dev mode
* build success
* dist folder existence

You MUST verify REAL deployed behavior on Vercel.

==================================================
MANDATORY CHECKS
================

Verify ALL of these carefully:

1. Does /sw.js actually exist publicly?

Open:
https://<domain>/sw.js

Verify:

* it returns REAL JavaScript
* NOT HTML
* NOT redirects
* NOT 404
* NOT Vercel fallback HTML

==================================================
2. VERIFY RESPONSE HEADERS
==========================

Check:

* Content-Type must be:
  application/javascript

Verify:

* no incorrect MIME types
* no cache corruption
* no Vercel rewrite conflicts

==================================================
3. VERIFY SERVICE WORKER REGISTRATION
=====================================

Audit:

* virtual:pwa-register setup
* registration timing
* registration scope
* registration mode
* production-only conditions
* immediate vs prompt registration
* whether registration silently fails

==================================================
4. VERIFY REAL BROWSER CONTROL
==============================

In Chrome DevTools:
Application →
Service Workers

Verify:

* service worker actually appears
* status becomes:
  activated and running
* page says:
  “This page is controlled by a service worker”

This is CRITICAL.

==================================================
5. VERIFY SW SCOPE
==================

Ensure:

* SW scope correctly controls root /
* not trapped under nested scope
* not broken by Vercel rewrites

==================================================
6. VERIFY BUILD OUTPUT
======================

Verify:

* dist/sw.js generated
* workbox files generated
* manifest generated
* all files actually deployed

==================================================
PHASE 2 — FIX THE ROOT CAUSE PROPERLY
=====================================

IMPORTANT:
Do NOT apply random hacks.

Identify the EXACT reason PWABuilder still cannot detect the SW.

==================================================
POSSIBLE FAILURE AREAS TO INVESTIGATE
=====================================

Audit ALL of these:

* Vercel rewrite rules interfering
* vite-plugin-pwa misconfiguration
* generateSW vs injectManifest problems
* SW registration path mismatch
* incorrect scope
* stale old SW conflicts
* incorrect caching headers
* SW not controlling page
* wrong registration lifecycle
* base path mismatch
* build artifact deployment mismatch

==================================================
IMPORTANT ENGINEERING DECISION
==============================

If vite-plugin-pwa is STILL behaving unreliably after proper debugging:

Then evaluate whether it is better to:

* keep vite-plugin-pwa with corrected config
  OR
* replace it with a more explicit/manual Workbox setup
  OR
* use a custom service worker approach

IMPORTANT:
DO NOT replace architecture blindly.

ONLY replace vite-plugin-pwa if:

* it is genuinely causing production instability
* or preventing reliable SW detection on Vercel

==================================================
PHASE 3 — OFFLINE SUPPORT VERIFICATION
======================================

PWABuilder also does not detect offline support.

Implement and VERIFY REAL offline functionality.

==================================================
REQUIREMENTS
============

Verify:

* app shell works offline
* navigation fallback works
* offline.html works
* no blank screens offline
* no crashes offline

==================================================
MANDATORY OFFLINE TEST
======================

In Chrome DevTools:

* disable network
* refresh app
* verify app still loads correctly

==================================================
PHASE 4 — PWABUILDER COMPATIBILITY VERIFICATION
===============================================

IMPORTANT:
After fixes are complete:

You MUST verify AGAIN on PWABuilder.

==================================================
TARGET RESULT
=============

PWABuilder should detect:

* Service Worker ✅
* Logic ✅
* Offline Support ✅

Manifest score should remain high.

==================================================
PHASE 5 — SCREENSHOT FIXES
==========================

PWABuilder currently says:
“Fix the links to your screenshots.”

Audit:

* manifest screenshot URLs
* file accessibility
* screenshot dimensions
* MIME types

Ensure screenshots:

* load publicly
* render correctly
* use proper manifest paths

==================================================
PHASE 6 — FINAL DOCUMENTATION UPDATE
====================================

IMPORTANT:
Update documentation AGAIN after final fixes.

DO NOT update only replit.md.

You MUST update:

* README.md
* PWA setup docs
* deployment notes
* troubleshooting notes

==================================================
DOCUMENT ALL OF THESE
=====================

* exact root cause
* why PWABuilder failed
* exact fix implemented
* service worker architecture
* deployment behavior
* Vercel compatibility setup
* caching strategy
* update lifecycle
* offline strategy
* troubleshooting steps
* screenshots setup
* known limitations

==================================================
FINAL VALIDATION (MANDATORY)
============================

Before finishing, FULLY verify:

==================================================
PWABUILDER
==========

* Has Service Worker ✅
* Has Logic ✅
* Offline Support ✅

==================================================
CHROME DEVTOOLS
===============

* active SW
* controlling page
* no registration errors
* no MIME errors
* no scope issues

==================================================
OFFLINE TESTING
===============

* app loads offline
* routes work offline
* no blank screens
* no crashes

==================================================
DEPLOYMENT TESTING
==================

* SW survives deploy
* update flow works
* no stale chunks
* no infinite reload loops

==================================================
FINAL REQUIRED DELIVERABLES
===========================

After completion provide:

1. EXACT root cause
2. EXACT fix implemented
3. Whether vite-plugin-pwa was kept or replaced
4. Why that decision was made
5. All files modified
6. How offline support works
7. How update lifecycle works
8. Why PWABuilder now detects the SW
9. Remaining limitations if any

Take your time.
Do this PROPERLY.
This is the FINAL service-worker/debugging pass.
